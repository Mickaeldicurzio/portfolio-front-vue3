import { GET_LANGUAGES } from "@/repositories/languages/getLanguages";
import { GET_LANGUAGES_BY_CATEGORY } from "@/repositories/languages/getLanguagesByCategory";

export const languagesRepository = {
    GET_LANGUAGES,
    GET_LANGUAGES_BY_CATEGORY
}