import { GET_COMPANIES } from "@/repositories/companies/getCompanies";
import { GET_ACTUAL_COMPANIES } from "@/repositories/companies/getCompaniesActual";

export const companiesRepository = {
    GET_COMPANIES,
    GET_ACTUAL_COMPANIES
}