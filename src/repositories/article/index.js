import { GET_ARTICLE_BY_ID } from "@/repositories/article/getArticleById";
import { GET_ARTICLES } from "@/repositories/article/getArticles";

export const articleRepository = {
    GET_ARTICLE_BY_ID,
    GET_ARTICLES
}